<% @user ||= current_user %>
<div class="border-top border-bottom pt-2 pb-3">
    <div class="d-flex justify-content-center mt-2">
        <div class="comments">
            <a href="<%= comments_path(@user) %>">
                <strong><%= @user.comments.count %></strong>&nbsp;Comments
            </a>
        </div>
        <div class="following">
            <a href="<%= following_user_path(@user) %>">
            <strong><%= @user.following.count %></strong>&nbsp;Following
            </a>
        </div>
        <div class="followers">
            <a href="<%= followers_user_path(@user) %>">
                <strong><%= @user.followers.count %></strong>&nbsp;Followers
            </a>
        </div>
        <%
=begin%>
 <div class="friends">
            <a href="<%= friends_path(@user) %>">
                <strong><%= @user.friends.count %></strong>&nbsp;Friends
            </a>
        </div> 
<%
=end%>
    </div>
</div>